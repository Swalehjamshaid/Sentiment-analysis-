[phases.setup]
# 'playwright-driver' provides the OS-level dependencies
nixPkgs = ["python311", "playwright-driver"]

[phases.install]
cmds = [
    "python -m venv /opt/venv",
    ". /opt/venv/bin/activate && pip install -r requirements.txt",
    # Use the specific library you have in requirements.txt (playwright or patchright)
    ". /opt/venv/bin/activate && playwright install chromium"
]

[start]
cmd = ". /opt/venv/bin/activate && gunicorn -w 1 -k uvicorn.workers.UvicornWorker app.main:app --bind 0.0.0.0:${PORT:-8080} --timeout 120"
