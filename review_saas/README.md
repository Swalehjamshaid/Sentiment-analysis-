# ReviewSaaS (FastAPI)

- FastAPI app with JWT cookies, email verification, 2FA-ready, companies, dashboard, exports stubs.
- Run: `uvicorn app.main:app --host 0.0.0.0 --port 8000`
- Configure via `.env` (see `.env.example`).
