# app/services/metrics.py

def aggregate_rating_distribution(metric_name: str, labels: dict = None):
    """
    Stub function for aggregate_rating_distribution.
    Replace with actual aggregation logic if needed.
    """
    labels = labels or {}
    # Dummy return
    return {"metric": metric_name, "labels": labels, "distribution": {}}
