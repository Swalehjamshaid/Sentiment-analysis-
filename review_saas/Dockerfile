# Manually point to the OpenSSL paths right before installing
RUN OPENSSL_LIB_DIR=/usr/lib/x86_64-linux-gnu \
    OPENSSL_INCLUDE_DIR=/usr/include/openssl \
    pip install --no-cache-dir -r requirements.txt
