# -------------------------------
# 🚀 FastAPI Backend Core
# -------------------------------
fastapi>=0.104.0          # Main web framework for APIs & dashboard backend
uvicorn[standard]>=0.23.0 # ASGI server to run FastAPI (includes websockets, reload, etc.)
gunicorn>=23.0.0          # Production-grade process manager (used in Railway/Docker)

# -------------------------------
# 🎨 Templates (FIXES YOUR ERROR)
# -------------------------------
jinja2>=3.1.2             # REQUIRED for Jinja2Templates (fixes your crash)
jinja2-time>=0.2.0        # Adds time/date filters inside templates

# -------------------------------
# 📦 Request Handling
# -------------------------------
python-multipart>=0.0.6   # Needed for form-data (file uploads, login forms)
requests>=2.31.0          # HTTP requests (APIs like Google Reviews, SerpAPI)
aiohttp>=3.9.2            # Async HTTP requests (faster scraping / APIs)

# -------------------------------
# 🧠 Data Validation & Settings
# -------------------------------
pydantic>=2.6.0           # Data validation (used heavily in FastAPI)
python-dotenv>=1.0.0      # Load environment variables (.env support)

# -------------------------------
# 🗄️ Database Layer
# -------------------------------
sqlalchemy>=2.0.0         # ORM for database operations
databases>=0.7.0          # Async DB support (optional but useful for FastAPI)

# -------------------------------
# 📊 Data Analysis (Dashboard Features)
# -------------------------------
pandas>=2.1.0             # Data processing (reviews, aggregation)
numpy>=1.26.0             # Numerical operations (used by pandas/sklearn)

# -------------------------------
# 📈 Visualization (Dashboard UI)
# -------------------------------
matplotlib>=3.8.0         # Graphs (fallback plotting)
seaborn>=0.12.0           # Statistical visualizations
plotly>=5.17.0            # Interactive dashboard charts (recommended)

# -------------------------------
# ☁️ Word Cloud & NLP
# -------------------------------
wordcloud>=1.9.2          # Word cloud generation
nltk>=3.8.1               # Text processing (sentiment prep, tokenization)

# -------------------------------
# 🤖 Machine Learning (Forecasting)
# -------------------------------
scikit-learn>=1.3.0       # Linear regression (ratings forecast, trends)

# -------------------------------
# 📤 Export Features
# -------------------------------
openpyxl>=3.1.0           # Export data to Excel (.xlsx files)

# -------------------------------
# 🌐 Google API Integration
# -------------------------------
google-api-python-client>=2.100.0  # Google APIs (reviews, places, etc.)
oauth2client>=4.1.3                # Authentication for Google APIs
